<div class="page-width">
  <h1>{{ blog.title }}</h1>
  {% for article in blog.articles %}
    <h2><a href="{{ article.url }}">{{ article.title }}</a></h2>
  {% endfor %}
</div>

{% schema %}
{
  "name": "Blog Section",
  "tag": "section",
  "settings": []
}
{% endschema %}
