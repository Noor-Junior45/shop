<div class="product-page">
  <h1>{{ product.title }}</h1>
  <div>{{ product.price | money }}</div>
  <div>{{ product.description }}</div>
</div>

{% schema %}
{
  "name": "Product Section",
  "tag": "section",
  "settings": []
}
{% endschema %}
